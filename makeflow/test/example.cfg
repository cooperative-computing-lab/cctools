[starch]
command	     = echo $(hostname) $(date $@) && cat $SFX_DIR/hosts.txt && my_alias
executables  = echo date hostname
libraries    = libmount.so.1
environments = example.env
data	     = hosts.txt:/etc/hosts localtime:/etc/localtime images:/usr/share/pixmaps/
