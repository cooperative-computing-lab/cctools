--------------------------------------------------------------------------------
All-Pairs: two simple examples -- bytewise comparison and summation
--------------------------------------------------------------------------------

The examples included here complete All-Pairs workloads for two types of functions. 
The first, bytewise comparison, is implemented natively within All-Pairs. 
The second, summation, is implemented (poorly) in a simple sum program provided.


The following will explain how to run each of the examples.

Setup and Execution
-------------------
No setup is required for the bytewise comparison other than making the allpairs tools:
allpairs /full/path/to/example/setDumbCompare/ /full/path/to/example/setDumbCompare/ compare_bitdumb testCompare

allpairs_wait testCompare.logfile

allpairs_cleanup -D ./my_results -F testCompare.finalize

allpairs_cleanup -R -L -M -F testCompare.finalize


Setup for the summation examplerequires compiling the sum program.
gcc sum.c -o sum.exe

Then:

allpairs /full/path/to/example/setSum/ /full/path/to/example/setSum/ /full/path/to/example/sum/ testSum

allpairs_wait testSum.logfile

allpairs_cleanup -D ./my_results -F /full/path/to/example/setSum/testSum.finalize

allpairs_cleanup -R -L -M -F /full/path/to/example/setSum/testSum.finalize

